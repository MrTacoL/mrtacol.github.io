const canvas=document.getElementById('weatherCanvas');const ctx=canvas.getContext('2d');const soundToggle=document.getElementById('soundToggle');const soundIcon=document.getElementById('soundIcon');const music=document.getElementById('music');let w=0,h=0,particles=[];function resize(){const r=Math.min(window.devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=Math.floor(w*r);canvas.height=Math.floor(h*r);canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(r,0,0,r,0,0);particles=Array.from({length:Math.floor(Math.min(95,Math.max(42,w/22)))},()=>({x:Math.random()*w,y:Math.random()*h,len:12+Math.random()*42,speed:2.2+Math.random()*5.5,alpha:.07+Math.random()*.16,drift:-.7+Math.random()*1.4}))}function draw(){ctx.clearRect(0,0,w,h);ctx.lineWidth=1;for(const p of particles){const glow=p.x>w*.44&&p.x<w*.9?'41,216,255':'255,255,255';ctx.strokeStyle=`rgba(${glow},${p.alpha})`;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x+p.drift*2.2,p.y+p.len);ctx.stroke();p.y+=p.speed;p.x+=p.drift;if(p.y>h+50){p.y=-50;p.x=Math.random()*w}}requestAnimationFrame(draw)}soundToggle.classList.add('disabled');soundToggle.title='Add your own legally usable music file to enable audio';soundToggle.addEventListener('click',async()=>{if(!music.querySelector('source')&&!music.src){soundIcon.textContent='🔇';setTimeout(()=>soundIcon.textContent='🔈',650);return}try{if(music.paused){await music.play();soundIcon.textContent='🔊';soundToggle.classList.remove('disabled')}else{music.pause();soundIcon.textContent='🔈'}}catch{soundIcon.textContent='🔇'}});addEventListener('resize',resize);resize();draw();